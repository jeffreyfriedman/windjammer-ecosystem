use std::env

/// Argv tokens after the program name.
pub fn read_args() -> Vec<string> {
    let all = env.args()
    let mut rest = Vec::new()
    let mut i = 1
    while i < all.len() {
        rest.push(all[i])
        i = i + 1
    }
    rest
}
