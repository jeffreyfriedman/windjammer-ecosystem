pub struct SchedulerConfig {
    pub default_max_minutes: int,
}

impl SchedulerConfig {
    pub fn defaults() -> SchedulerConfig {
        SchedulerConfig {
            default_max_minutes: 1440,
        }
    }
}

pub fn config_from_env() -> SchedulerConfig {
    SchedulerConfig::defaults()
}
